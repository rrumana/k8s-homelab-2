apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: media

resources:
  - externalsecret.yaml
  - postgres-configmap.yaml
  - servarr-postgres-bootstrap-configmap.yaml
  - pvc.yaml
  - pf-sync-configmap.yaml
  - deployment.yaml
  - service.yaml
  - migrate-servarr-sqlite-to-postgres-job.yaml

commonLabels:
  app.kubernetes.io/name: arr
  app.kubernetes.io/managed-by: argocd
