apiVersion: apps/v1
kind: Deployment
metadata:
  name: librechat-rag-api
  labels:
    app: librechat-rag-api
spec:
  replicas: 2
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: librechat-rag-api
  template:
    metadata:
      annotations:
        linkerd.io/inject: enabled
        config.linkerd.io/skip-outbound-ports: "6379"
      labels:
        app: librechat-rag-api
    spec:
      serviceAccountName: librechat-rag-api
      containers:
        - name: rag-api
          image: harbor.rcrumana.xyz/proxy-ghcr/danny-avila/librechat-rag-api-dev-lite:65c64ed8bca3b1b7f259382570e98586e530c1be
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8000
          env:
            - name: HOST
              value: 0.0.0.0
            - name: PORT
              value: "8000"
            - name: RAG_PORT
              value: "8000"
            - name: RAG_VECTOR_STORE
              value: pgvector
            - name: RAG_OPENAI_BASEURL
              value: http://llm-gateway.ai.svc.cluster.local:4000/v1
            - name: DB_HOST
              value: pg-ai-rw.databases.svc.cluster.local
            - name: DB_PORT
              value: "5432"
            - name: POSTGRES_DB
              value: librechat_rag
            - name: POSTGRES_USER
              value: librechat_rag
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: librechat-secret
                  key: PGVECTOR_PASSWORD
            - name: RAG_OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: librechat-secret
                  key: OPENAI_API_KEY
            - name: PGVECTOR_DB_URL
              valueFrom:
                secretKeyRef:
                  name: librechat-secret
                  key: PGVECTOR_DB_URL
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: librechat-secret
                  key: REDIS_PASSWORD
            - name: REDIS_URL
              value: redis://:$(REDIS_PASSWORD)@valkey-cache-primary.databases.svc.cluster.local:6379
          resources:
            requests:
              cpu: "300m"
              memory: "512Mi"
            limits:
              cpu: "2"
              memory: "4Gi"
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
