apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cmd-params-cm
data:
  # Terminate TLS at the ingress and serve HTTP from argocd-server.
  # This avoids redirect loops if the ingress does not set X-Forwarded-Proto.
  server.insecure: "true"
