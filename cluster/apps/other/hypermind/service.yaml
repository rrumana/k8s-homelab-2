apiVersion: v1
kind: Service
metadata:
  name: hypermind
  labels:
    app: hypermind
spec:
  type: ClusterIP
  selector:
    app: hypermind
  ports:
    - name: http
      port: 80
      targetPort: http
