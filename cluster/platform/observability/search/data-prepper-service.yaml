apiVersion: v1
kind: Service
metadata:
  name: data-prepper
  namespace: search
  labels:
    app.kubernetes.io/name: data-prepper
spec:
  selector:
    app.kubernetes.io/name: data-prepper
  ports:
    - name: http
      port: 2021
      targetPort: http
    - name: metrics
      port: 4900
      targetPort: metrics
