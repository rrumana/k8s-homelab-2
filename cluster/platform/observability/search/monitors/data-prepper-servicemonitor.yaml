apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: data-prepper
  namespace: search
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: data-prepper
  endpoints:
    - port: metrics
      path: /metrics/prometheus
      interval: 30s
