apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: llama-models-cache
spec:
  storageClassName: ceph-filesystem
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
