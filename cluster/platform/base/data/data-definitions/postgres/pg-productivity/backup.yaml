apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: pg-productivity-daily
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  schedule: "0 45 3 * * *"
  backupOwnerReference: cluster
  cluster:
    name: pg-productivity
  immediate: true
