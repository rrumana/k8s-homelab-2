apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: other

resources:
  - serviceaccount.yaml
  - deployment.yaml
  - service.yaml
  - vpa.yaml

commonLabels:
  app.kubernetes.io/name: hypermind
  app.kubernetes.io/managed-by: argocd
