apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: headscale-postgres
  namespace: other
  annotations:
    argocd.argoproj.io/sync-wave: "-20"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault
    kind: ClusterSecretStore
  target:
    name: headscale-postgres
    creationPolicy: Owner
  data:
    - secretKey: database
      remoteRef:
        key: apps/other/headscale-postgres
        property: database
    - secretKey: password
      remoteRef:
        key: apps/other/headscale-postgres
        property: password
    - secretKey: username
      remoteRef:
        key: apps/other/headscale-postgres
        property: username
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: harbor-pull-creds
  namespace: other
  annotations:
    argocd.argoproj.io/sync-wave: "-20"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault
    kind: ClusterSecretStore
  target:
    name: harbor-pull-creds
    creationPolicy: Owner
    template:
      type: kubernetes.io/dockerconfigjson
  data:
    - secretKey: .dockerconfigjson
      remoteRef:
        key: apps/other/harbor-pull-creds
        property: .dockerconfigjson
