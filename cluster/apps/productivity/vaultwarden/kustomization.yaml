apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: productivity

resources:
  - externalsecret.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  # Intentionally excluded for now:
  # - migrate-vaultwarden-sqlite-to-shared-postgres.yaml
  # Cutover switch:
  # - deployment-cutover-shared-postgres.yaml (replace deployment.yaml)

commonLabels:
  app.kubernetes.io/name: vaultwarden
  app.kubernetes.io/managed-by: argocd
