apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: nextcloud-admin
  namespace: productivity
  annotations:
    argocd.argoproj.io/sync-wave: "-20"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault
    kind: ClusterSecretStore
  target:
    name: nextcloud-admin
    creationPolicy: Owner
  data:
    - secretKey: nextcloud-username
      remoteRef:
        key: apps/productivity/nextcloud-admin
        property: nextcloud-username
    - secretKey: nextcloud-password
      remoteRef:
        key: apps/productivity/nextcloud-admin
        property: nextcloud-password
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: nextcloud-db-shared
  namespace: productivity
  annotations:
    argocd.argoproj.io/sync-wave: "-20"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault
    kind: ClusterSecretStore
  target:
    name: nextcloud-db-shared
    creationPolicy: Owner
  data:
    - secretKey: host
      remoteRef:
        key: apps/productivity/nextcloud-db-shared
        property: host
    - secretKey: port
      remoteRef:
        key: apps/productivity/nextcloud-db-shared
        property: port
    - secretKey: database
      remoteRef:
        key: apps/productivity/nextcloud-db-shared
        property: database
    - secretKey: username
      remoteRef:
        key: apps/productivity/nextcloud-db-shared
        property: username
    - secretKey: password
      remoteRef:
        key: apps/productivity/nextcloud-db-shared
        property: password
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: nextcloud-redis-shared
  namespace: productivity
  annotations:
    argocd.argoproj.io/sync-wave: "-20"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault
    kind: ClusterSecretStore
  target:
    name: nextcloud-redis-shared
    creationPolicy: Owner
  data:
    - secretKey: password
      remoteRef:
        key: apps/databases/valkey-cache-auth
        property: password
