apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: linkerd2-cni
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  project: default
  sources:
    - repoURL: oci://harbor.rcrumana.xyz/thirdparty-charts/linkerd2-cni
      targetRevision: "2026.3.3"
      path: .
      helm:
        releaseName: linkerd2-cni
        valueFiles:
          - $values/cluster/platform/service-mesh/linkerd/cni/values.yaml
    - repoURL: https://github.com/rrumana/k8s-homelab-2.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: service-mesh
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
