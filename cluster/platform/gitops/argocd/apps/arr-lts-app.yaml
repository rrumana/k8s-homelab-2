# platform/gitops/argocd/apps/arr-lts-app.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: arr-lts
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/rrumana/k8s-homelab-2.git
    targetRevision: main
    path: cluster/apps/media/arr-lts

  destination:
    server: https://kubernetes.default.svc
    namespace: media

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
