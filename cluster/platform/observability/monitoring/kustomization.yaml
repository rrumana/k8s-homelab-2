apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  - harbor-pull-creds.yaml
  - opensearch-admin-credentials.yaml
  - grafana-dashboard-linkerd.yaml
  - grafana-dashboard-logging-stack.yaml

commonLabels:
  app.kubernetes.io/part-of: observability-monitoring
  app.kubernetes.io/managed-by: argocd
