apiVersion: v1
kind: Service
metadata:
  name: headscale-ui
  labels:
    app: headscale
    component: ui
spec:
  selector:
    app: headscale
    component: ui
  ports:
    - name: http
      port: 80
      targetPort: 8080
